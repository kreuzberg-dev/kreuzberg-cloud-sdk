---
title: Changelog
description: Notable changes to the Xberg Python, TypeScript and Go clients.
---

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Each client publishes
its own package version; this page tracks changes that affect more than one language, or the API
surface documented across this site.

## [Unreleased]

### Added

- Per-file extraction config overrides (`documents[].config` / `config-<filename>`), documented in
  [Per-file configs](/guides/per-file-configs/).
