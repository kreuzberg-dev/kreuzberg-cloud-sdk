packages:
  - "."
  - "packages/typescript"
allowBuilds:
  esbuild: true
  msw: true

# pnpm settings migrated from .npmrc (npm warns on kebab-case keys it does not recognize)
sharedWorkspaceLockfile: true
autoInstallPeers: true
hoist: true
